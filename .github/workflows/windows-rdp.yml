name: Free Windows RDP via Tailscale

on:
  workflow_dispatch:
  repository_dispatch:
    types: [rerun_rdp]

permissions:
  contents: write
  actions: write # إضافة صلاحية مسح الـ Actions تلقائياً

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      # 1. تثبيت تايل سكيل عبر مدير حزم ويندوز المدمج (Choco)
      - name: Install Tailscale via Chocolatey
        shell: powershell
        run: |
          choco install tailscale -y

      # 2. الربط بالشبكة
      - name: Connect to Tailscale Network
        shell: powershell
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
          Start-Sleep -Seconds 5
          if (Test-Path "C:\Program Files\Tailscale\tailscale.exe") {
              & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=$env:TAILSCALE_AUTH_KEY --accept-routes
          } else {
              tailscale up --authkey=$env:TAILSCALE_AUTH_KEY --accept-routes
          }

      # 3. إعداد الـ RDP والمستخدم
      - name: Enable RDP and Setup User
        shell: powershell
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          $Password = ConvertTo-SecureString "GitHubRDP@2026" -AsPlainText -Force
          Set-LocalUser -Name "runneradmin" -Password $Password

      # 4. تثبيت أدواتك وبرامجك تلقائياً (بالإنجليزية لتفادي خطأ الترميز)
      - name: Auto-Install My Tools & Apps
        shell: powershell
        run: |
          Write-Host "Installing Google Chrome and WinRAR..."
          choco install googlechrome -y
          choco install winrar -y

      # 5. الحفاظ على تشغيل السيرفر (5 ساعات و50 دقيقة)
      - name: Keep Alive Loop
        shell: powershell
        run: |
          $minutes = 350
          for ($i = 0; $i -lt $minutes; $i++) {
              Start-Sleep -Seconds 60
          }

      # 6. خطوة استدعاء التشغيل القادم تلقائياً
      - name: Trigger Next Run
        shell: powershell
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $repo = "${{ github.repository }}"
          $url = "https://api.github.com/repos/$repo/dispatches"
          $headers = @{
              "Authorization" = "Bearer $env:GITHUB_TOKEN"
              "Accept"        = "application/vnd.github.v3+json"
          }
          $body = @{
              "event_type" = "rerun_rdp"
          } | ConvertTo-Json
          Invoke-RestMethod -Uri $url -Method Post -Headers $headers -Body$body -ContentType "application/json"

      # 7. [خطوة جديدة] مسح سجل التشغيل الحالي تلقائياً لتنظيف الصفحة
      - name: Auto Delete Current Workflow Run
        shell: powershell
        if: always() # تعمل دائماً حتى لو حدث خطأ في الخطوات السابقة
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $repo = "${{ github.repository }}"
          $run_id = "${{ github.run_id }}"
          $url = "https://api.github.com/repos/$repo/actions/runs/$run_id"
          $headers = @{
              "Authorization" = "Bearer $env:GITHUB_TOKEN"
              "Accept"        = "application/vnd.github.v3+json"
          }
          Write-Host "Deleting workflow run $run_id to keep history clean..."
          Invoke-RestMethod -Uri $url -Method Delete -Headers$headers
