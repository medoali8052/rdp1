name: Free Windows RDP via Tailscale

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      # 1. تثبيت تايل سكيل عبر مدير حزم ويندوز المدمج (Choco) - سريع ومستقر جداً
      - name: Install Tailscale via Chocolatey
        shell: powershell
        run: |
          choco install tailscale -y

      # 2. الربط بالشبكة (انتظار 5 ثوانٍ ليتفعل مسار البرنامج في الويندوز)
      - name: Connect to Tailscale Network
        shell: powershell
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          Start-Sleep -Seconds 5
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=$env:TAILSCALE_AUTH_KEY --accept-routes

      # 3. إعداد الـ RDP والمستخدم
      - name: Enable RDP and Setup User
        shell: powershell
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          
          $Password = ConvertTo-SecureString "GitHubRDP@2026" -AsPlainText -Force
          Set-LocalUser -Name "runneradmin" -Password $Password

      # 4. الحفاظ على تشغيل السيرفر
      - name: Keep Alive Loop
        shell: powershell
        run: |
          $hours = 6
          for ($i = 0; $i -lt ($hours * 60); $i++) {
              Start-Sleep -Seconds 60
          }
